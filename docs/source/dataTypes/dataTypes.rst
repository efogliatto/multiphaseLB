Data types
==========

Data types used across the code.

Most of them are structures containing information. In the special case of fields (e.g. lbeField), the structure holds a field array and related parameters.


.. toctree::

   basicMesh
   EOSInfo
   latticeInfo
   latticeMesh
   lbeField
   macroFields
   mpiInfo
   timeInfo
   vtkInfo


